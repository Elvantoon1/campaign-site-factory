{
  "name": "campaign-site-factory",
  "version": "2.0.0",
  "description": "Factory Control Plane — runs as a persistent Node service on Render, provisions isolated Vercel (frontend) + Render Postgres (database) deployments for new campaign-site tenants from the shared template.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node lib/db.test.js",
    "generate-encryption-key": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "engines": {
    "node": "18.x"
  }
}
